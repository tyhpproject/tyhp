# Story 14 — diagnostic golden fixtures

Representative inputs that intentionally trigger diagnostics. Manifest expectations cover
code + error count; richer span/suggestion snapshots remain covered by unit tests under
`tests/Tyhp.Tests/Diagnostics/` (renderer, DidYouMean, JSON/SARIF).
