<?tyhp

namespace TestData\Parser;

function sampleExitForms(): void
{
    exit;
    exit();
    exit(0);
    exit(...);
    exit(status: 0);

    die;
    die();
    die(1);
    die(...);
    die(status: 1);
}
