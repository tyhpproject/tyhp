<?tyhp

namespace TestData\Binder;

function nestedScopes(int $limit): int
{
    int $total = 0;

    for ($i = 0; $i < $limit; $i++) {
        if ($i % 2 === 0) {
            foreach ([1, 2] as $item) {
                $total += $item;
            }
        }
    }

    return $total;
}
