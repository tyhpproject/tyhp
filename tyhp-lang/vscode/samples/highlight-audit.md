# Tyhp fence highlighting

```tyhp
<?tyhp

class Greeter {
    function hello(string $name): string {
        return "Hello ${string}, $name";
    }
}
```

```tyhpdef
<?tyhpdef

function strlen(string $string): int;
```
